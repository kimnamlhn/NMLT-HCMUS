#include <stdio.h>
#include <conio.h>
int main(void)
{
	int a, b, c, d, t1, t2, t3, t4;
	printf("Nhap 4 so nguyen: ");
	scanf_s("%d %d %d %d", &a, &b, &c, &d);
	if (a > b)
	{
		if
		{

		}
	}
   
	printf("Sap xep gia tri 4 so nguyen theo thu tu tang dan:\n %d > %d > %d > %d", t1, t2, t3, t4);
	_getch();
}